# Add project specific ProGuard rules here.
# minifyEnabled is false for both build types in this slice (see app/build.gradle.kts),
# so this file is currently unused, but kept present since release{} references it.
